# 这段内容展示 Studio 写入用户级 config.toml 的网关字段。
model = "codex-chat"
model_provider = "local-chat-gateway"

[model_providers.local-chat-gateway]
name = "Local Chat-to-Responses Gateway"
base_url = "http://127.0.0.1:4000/v1"
wire_api = "responses"
